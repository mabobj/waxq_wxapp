package com.wxapp.frame.base;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

public class BaseAction {
    protected Logger logger = LogManager.getLogger(this.getClass().getName());

}
